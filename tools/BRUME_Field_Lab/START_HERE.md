# START HERE — BRUME FIELD LAB

If you only read one file before running the tool, read this one.

## What this is

BRUME Field Lab is a Processing 4 lettermark design instrument. It is built to help design the BRUME `B` by manipulating **real font-outline geometry**, not by applying a filter to a screenshot of a letter.

## Fastest path to first run

1. Install Processing 4.
2. Keep the `BRUME_Field_Lab` folder intact.
3. Open `BRUME_Field_Lab.pde` in Processing's Java mode.
4. Press Run.
5. Press `F` or click **LOAD FONT** and choose a `.ttf` or `.otf`.
6. Leave the glyph as `B` for your first session.
7. Select **Wave Field** and experiment with Amplitude, Frequency, Focus Y, and Spread.
8. Hold `Space` often to compare the procedural result with the hand-edited master.
9. Use Sculpt and Node modes to turn interesting accidents into intentional geometry.
10. Export SVG when the letterform itself works; use raster effects only after that.

## Read next

- `README.md` — complete operating manual.
- `docs/CODE_WALKTHROUGH_ELI5.md` — code and architecture explained without assuming programming experience.
- `docs/BUILD_AND_DESIGN_SHEET.md` — product direction and future roadmap.
- `docs/TROUBLESHOOTING.md` — what to do if Processing throws an error.

**Core rule: form first; treatment second.**
